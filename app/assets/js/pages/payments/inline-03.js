/* Extracted from app/payments.html — GDprint v8.0 */
document.body.insertAdjacentHTML('beforeend',desktop());(async()=>{if(!await requireCustomer())return;try{const orders=await loadMyOrders(100),box=document.getElementById('payment-list');box.innerHTML=orders.length?orders.map(o=>`<a class="card payment-list-card" href="order.html?no=${encodeURIComponent(o.order_number)}"><div><div class="order-no">#${esc(o.order_number)}</div><div class="meta">${esc(o.service_name||'Պատվեր')}</div></div><div class="payment-right"><b>${Number(o.total_amount)>0?money(o.total_amount):'Պայմանագրային'}</b><span class="payment-status ${o.payment_status==='paid'?'paid':''}">${esc(paymentStatusLabel(o.payment_status))}</span></div></a>`).join(''):'<div class="card empty-state"><b>Պատվերներ դեռ չկան</b></div>'}catch(e){showAppMessage(e.message)}})();
