/* Extracted from app/upload.html — GDprint v8.0 */
document.body.insertAdjacentHTML('beforeend',desktop());let uploaded=JSON.parse(sessionStorage.getItem('gdUploadedFiles')||'[]');function draw(){document.getElementById('uploaded').innerHTML=uploaded.length?'<div class="card" style="padding:0 14px">'+uploaded.map(x=>`<div class="file-row"><div><b>${esc(x.file_name)}</b><div class="meta">${Math.round((x.file_size||0)/1024)} KB</div></div><span class="status-chip">Վերբեռնված</span></div>`).join('')+'</div>':'<div class="empty-state"><b>Ֆայլ դեռ չկա</b></div>'}(async()=>{if(!await requireCustomer())return;if(!sessionStorage.getItem('gdOrderDraft')){location.href='services.html';return}draw();document.getElementById('upload-btn').onclick=async()=>{const fs=[...document.getElementById('files').files];if(!fs.length)return showAppMessage('Ընտրեք առնվազն մեկ ֆայլ');for(const f of fs){if(f.size>50*1024*1024)return showAppMessage(f.name+' ֆայլը գերազանցում է 50MB-ը')}const b=document.getElementById('upload-btn');b.disabled=true;b.textContent='Վերբեռնվում է...';try{for(const f of fs){const meta=await uploadDraftFile(f);uploaded.push(meta);sessionStorage.setItem('gdUploadedFiles',JSON.stringify(uploaded));draw()}showAppMessage('Ֆայլերը հաջողությամբ վերբեռնվել են','success');document.getElementById('files').value=''}catch(e){showAppMessage(e.message)}finally{b.disabled=false;b.textContent='Վերբեռնել ընտրված ֆայլերը'}}})();
