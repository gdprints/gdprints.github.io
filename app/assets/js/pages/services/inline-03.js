/* Extracted from app/services.html — GDprint v8.0 */
document.body.insertAdjacentHTML('beforeend',desktop());const box=document.getElementById('service-list');const renderServices=q=>{const qq=(q||'').trim().toLowerCase();const services=(window.GD&&Array.isArray(window.GD.services))?window.GD.services:[];box.innerHTML=services.filter(s=>(s[1]+' '+s[2]).toLowerCase().includes(qq)).map(s=>`<a class="card service" href="service.html?key=${s[0]}">${serviceImage(s[3],s[1])}<div><h3>${s[1]}</h3><p>${s[2]}</p></div><span class="chev">›</span></a>`).join('')||'<div class="card empty-state"><b>Ծառայություն չի գտնվել</b></div>'};renderServices('');document.getElementById('search').addEventListener('input',e=>renderServices(e.target.value));(async()=>{await requireCustomer()})();
